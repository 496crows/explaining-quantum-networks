# Core simulation / math
matplotlib>=3.10.7
numpy>=2.3.5
scikit-learn
scipy>=1.16.3
scipy-stubs~=1.16.3

# SeQUeNCe quantum-network simulator (read-only submodule).
# Pinned to upstream SeQUeNCe v1.0.0 (sequence-toolbox/SeQUeNCe). The two former
# local patches are retired and preserved in sequence-submodule-2-commits.patch;
# the pre-swap Eve memory hook they provided now lives first-party in
# sequence_game/sequence_build/pre_swap_swapping.py. Run:
#   git submodule update --init
# The editable install pulls SeQUeNCe's own deps (numpy, qutip, stim, ...).
-e ./sequence-toolbox-SeQUeNCe

# Test runner
pytest
